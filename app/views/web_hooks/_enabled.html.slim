- if @enabled_web_hooks.size > 0
  table.table.table-bottom-borderless.mt-2
    - @enabled_web_hooks.each do |web_hook|
      == render 'web_hooks/item', current_channel: @channel, web_hook: web_hook, type: :enabled
- else
  == render 'web_hooks/not_found'
